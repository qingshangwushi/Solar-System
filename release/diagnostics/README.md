# Diagnostics

诊断工具与基准测试产物（设计文档 31.4 / 29.1）。

## 内容

- `benchmark.json` — GPU 基准测试结果
- `diagnose.sh` — 诊断脚本
- `verify.sh` — 验证脚本

## 来源

由 `packages/diagnostics` 编译生成，或由 `tools/build-release.sh` 聚合。
