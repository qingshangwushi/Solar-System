{
  "name": "@solar-system/astro-core-wasm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build:wasm": "wasm-pack build ../../crates/astro-core --target web --out-dir ../../packages/astro-core-wasm/pkg",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
